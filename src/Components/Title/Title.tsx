import React from 'react';
import { StyledTitle } from '../../styled-components';

const Title = () => {
  return (
    <StyledTitle>
      <h1 data-text="Redux-Observable">Redux-Observable</h1>

      <span>Guía rápida para implementar Redux-Observable + Redux Toolkit</span>
    </StyledTitle>
  );
};

export default Title;
